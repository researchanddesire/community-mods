name: Build Project Hub gallery

on:
  push:
    branches: [main]
    paths:
      - 'mods/**'
      - 'CONTRIBUTING.md'
      - '.github/workflows/scripts/build_gallery.py'
      - '.github/workflows/scripts/assets/**'
      - '.github/workflows/gallery.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: Install dependencies
        run: pip install pyyaml jsonschema markdown nh3 'packaging>=24.2'
      - name: Validate project catalog
        run: python .github/workflows/scripts/mod_lint.py
      - name: Run catalog regression tests
        run: python -m unittest discover -s .github/workflows/scripts -p 'test_*.py'
      - name: Build project gallery
        run: python .github/workflows/scripts/build_gallery.py
      - name: Add CNAME
        run: echo 'mods.researchanddesire.com' > site/CNAME
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
